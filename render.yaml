# Render Blueprint deployment config
services:
  - type: web
    name: hanxuan-shuizitaosha
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /healthz
    autoDeploy: true
